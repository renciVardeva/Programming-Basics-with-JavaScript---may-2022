function operationsBetweenNumbers(input){
    let n1 = Number (input[0]);
    let n2 = Number (input[1]);
    let op = input[2];
    let result = 0;

    if ( result % 2 === 0 && op === "+" || op === "-" || op === "*"){
        result =
    }
}